<?xml version="1.0" encoding="utf-8"?>
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:padding="16dp">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="5dp"
        android:orientation="vertical">

        <!-- Título -->
        <TextView
            android:id="@+id/tvGoalsTitle"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="20dp"
            android:paddingBottom="16dp"
            android:text="Objetivo semanal"
            android:textAlignment="center"
            android:textSize="24sp" />

        <!-- Card con datos principales -->
        <androidx.cardview.widget.CardView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginBottom="16dp"
            android:elevation="4dp"
            android:padding="16dp">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:layout_margin="10dp"
                android:orientation="vertical">

                <TextView
                    android:id="@+id/tvWeeklyGoal"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:paddingBottom="4dp"
                    android:text="Objetivo semanal: -- min" />

                <!-- Estado/porcentaje -->
                <TextView
                    android:id="@+id/tvWeeklyStatus"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:paddingBottom="8dp"
                    android:text="Progreso: -- / -- min (--%)" />

                <ProgressBar
                    android:id="@+id/progressWeekly"
                    style="?android:attr/progressBarStyleHorizontal"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:max="100"
                    android:paddingBottom="8dp"
                    android:progress="0" />

                <TextView
                    android:id="@+id/tvCurrentStreak"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:paddingBottom="8dp"
                    android:text="Racha actual: -- días" />

                <!-- Botón para cambiar objetivo -->
                <Button
                    android:id="@+id/btnChangeGoal"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_gravity="end"
                    android:text="Cambiar objetivo" />

            </LinearLayout>

        </androidx.cardview.widget.CardView>

        <!-- Placeholder gráfica -->
        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingTop="8dp"
            android:text="(Gráfico próximamente)"
            android:textAlignment="center"
            android:textSize="16sp"
            android:visibility="gone" />

    </LinearLayout>

</ScrollView>
